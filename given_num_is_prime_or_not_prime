function isPrime(num){
    if(num <= 2) return false;
    for(i=2; i<=Math.sqrt(num); i++){
        if(num % i === 0) return false;
    }
    return true
}
 let num = parseInt(prompt('enter the number: '));
 console.log(`${num} is ${isPrime(num) ? 'prime':'not_prime'}`)
